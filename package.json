{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "opencode2-cliproxyapi",
  "version": "0.1.0",
  "description": "Discover and use every model exposed by CLIProxyAPI in OpenCode V2.",
  "type": "module",
  "license": "MIT",
  "author": "İbrahim BABAL",
  "contributors": [
    "Ben Brougher"
  ],
  "homepage": "https://github.com/moutansos/opencode-cliproxyapi#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/moutansos/opencode-cliproxyapi.git"
  },
  "bugs": {
    "url": "https://github.com/moutansos/opencode-cliproxyapi/issues"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun --format esm --external @opencode/plugin && tsc --emitDeclarationOnly",
    "typecheck": "tsc --noEmit",
    "test": "bun test ./src",
    "check": "bun run typecheck && bun run test && bun run build",
    "prepublishOnly": "bun run check"
  },
  "keywords": [
    "opencode",
    "opencode-v2",
    "cliproxyapi",
    "llm",
    "plugin"
  ],
  "peerDependencies": {
    "@opencode/plugin": ">=2.0.0"
  },
  "devDependencies": {
    "@opencode/plugin": "2.0.4",
    "@types/bun": "^1.3.5",
    "typescript": "^5.9.3"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
